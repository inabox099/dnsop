apiVersion: 1

providers:
  - name: 'DNS Observability Dashboards'
    orgId: 1
    folder: 'DNS'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
